---
model: haiku
---

Switch to main or master branch (whichever exists) and pull latest changes.

1. Check if `main` branch exists locally or on remote
2. If `main` exists, switch to it
3. Otherwise, check if `master` exists and switch to it
4. Pull the latest changes from remote
5. Report the result
